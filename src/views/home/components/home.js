import React, { PropTypes, Component } from 'react';
import './home.scss';

export default class Home extends Component {

    static propTypes = {
    };

    render() {
        return (
            <div className='home'>
                <header>Archivist</header>
            </div>
        );
    }
}
