import React, { PropTypes, Component } from 'react';
// import './notes-tab.scss';

export default class NotesTab extends Component {

    static propTypes = {
    };

    render() {
        return (
            <div className='notes-tab'>
                these are where notes go
            </div>
        );
    }
}
